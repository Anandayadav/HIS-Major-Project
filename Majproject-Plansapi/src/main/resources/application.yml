server:
  port: 8082
  
spring:
  datasource:
    password: Anandak@97
    url: jdbc:mysql://localhost:3306/anandadb
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true